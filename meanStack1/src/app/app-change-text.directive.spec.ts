import { AppChangeTextDirective } from './app-change-text.directive';

describe('AppChangeTextDirective', () => {
  it('should create an instance', () => {
    const directive = new AppChangeTextDirective();
    expect(directive).toBeTruthy();
  });
});
